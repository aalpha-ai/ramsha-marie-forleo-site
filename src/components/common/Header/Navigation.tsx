/**
 * This code was generated by Builder.io.
 */
import React from "react";
import NavigationItem from "./NavigationItem";

interface NavItem {
  label: string;
  href?: string;
  hasDropdown?: boolean;
}

interface NavigationProps {
  isMobile?: boolean;
}

export const navItems: NavItem[] = [
  { label: "Blog" },
  { label: "Work With Me" },
  { label: "In The Press" },
  { label: "About" },
  // { label: "Courses", hasDropdown: true },
  // { label: "MarieTV", href: "https://www.marieforleo.com/marietv" },
  // { label: "Podcast", href: "https://www.marieforleo.com/podcast" },
  // { label: "About", href: "https://www.marieforleo.com/about" },
  // { label: "More", hasDropdown: true },
];

const Navigation: React.FC<NavigationProps> = ({ isMobile }) => {
  return (
    <nav className={`lg:text-[14px] xl:text-[16px] font-[600] ${isMobile ? 'flex flex-col w-full' : 'flex items-center justify-between w-full max-w-[600px]'}`}>
      {navItems.map((item, index) => (
        <NavigationItem key={index} {...item} />
      ))}
    </nav>
  );
};

export default Navigation;
