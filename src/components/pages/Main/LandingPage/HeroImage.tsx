/**
 * This code was generated by Builder.io.
 */
'use client'
import React from "react";
import Image from "next/image";
import { motion } from "framer-motion";

const HeroImage: React.FC = () => {
  return (
    <motion.div
      initial={{ 
        opacity: 0,
        y: 20,
        scale: 0.95
      }}
      animate={{ 
        opacity: 1,
        y: 0,
        scale: 1
      }}
      transition={{
        duration: 0.8,
        ease: [0.22, 1, 0.36, 1], // Custom easing for smooth entrance
        delay: 0.2
      }}
    >
      <Image
        src="/hero-2.webp"
        alt="Ramsha's portrait"
        className="w-full h-full"
        width={500}
        height={500}
        priority // Add priority since this is hero image
        quality={100} // Ensure highest quality for hero image
      />
    </motion.div>
  );
};

export default HeroImage;
